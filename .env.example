# Copy this file to .env and adjust values as needed
# Environment variables used by the app and docker-compose

# --- Providers ---
DB_PROVIDER=POSTGRES
LLM_PROVIDER=GEMINI

# --- Database connection (from host) ---
DB_HOST=127.0.0.1
DB_PORT=5433
DB_NAME=local_llm
DB_USER=local_llm
DB_PASSWORD=local_llm

# --- LLM provider (Gemini) ---
# Get an API key: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=REPLACE_WITH_YOUR_KEY
